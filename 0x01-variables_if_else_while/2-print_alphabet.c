#include <stdio.h>
/**
 * main - prints the alphabet in lowercase,followed by a new line
 *
 * Return: 0
 */
int main(void)
{
	for (ch = 'a'; ch <= 'z'; ch++)
	{
	printf("%c ", ch);
	}

	return (0);
}
