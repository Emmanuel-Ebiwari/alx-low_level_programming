#include "main.h"
/**
 * rev_string - reverses a string.
 *
 * @s: string
 */
void rev_string(char *s)
{
	int length;
	
	length = _strlen(s);

	_putchar(length + '0');

/*	for (i=length; i >= 0; i--) */
		
		
}
